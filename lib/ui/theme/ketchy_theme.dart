
import 'package:flutter/material.dart';

final ketchyColorScheme = ColorScheme.fromSeed(seedColor: Colors.deepPurple);

final ketchyTheme = ThemeData(
  colorScheme: ketchyColorScheme,
  useMaterial3: true,
);